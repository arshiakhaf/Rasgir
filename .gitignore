# --- Build outputs ---
build/
*/build/
.gradle/
local.properties
*.iml
.idea/
.kotlin/

# --- Local caches / toolchains (never commit) ---
.cache/
*.whl
*.tgz

# --- Local scratch ---
scratch/
*.log

# --- Keys are delivered under deliverables/keys explicitly (see docs) ---
# (keystores are intentionally committed there as part of the delivery bundle;
#  protect this repository accordingly.)
